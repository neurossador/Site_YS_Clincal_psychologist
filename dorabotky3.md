Исправь баг в верхней фиксированной шапке сайта.

Проблема: в header произошло визуальное наложение ФИО «Хмарук Юлия Сергеевна» на пункты меню «Обо мне», «Документы», «Услуги». На некоторых ширинах экрана логотип клиники, ФИО, навигация и кнопка переключения темы не помещаются в одну строку. В результате элементы header конфликтуют и перекрывают друг друга.

Нужно исправить адаптивность шапки, не ломая текущий дизайн сайта.

Проверь и исправь:

1. `.header__inner`, `.header__brand`, `.logo`, `.header__actions`, `.nav`, `.nav-toggle`.
2. Убери наложение ФИО на навигацию.
3. На широком экране шапка должна оставаться в одну строку, но без перекрытия.
4. На средних и малых ширинах меню должно переходить в корректный мобильный режим.
5. Если используется бургер-меню, сделай его полностью рабочим: `.nav-toggle` должен отображаться на нужном breakpoint, `.nav.is-open` должен иметь CSS-правила, а пункты меню должны открываться/закрываться без наложения.
6. ФИО можно аккуратно ограничить по ширине через `max-width`, `overflow: hidden`, `text-overflow: ellipsis` или перенести в более компактный формат на средних экранах.
7. Не удаляй пункты меню и не меняй структуру секций.
8. Проверь header на ширинах 1440px, 1280px, 1024px, 768px, 430px и 390px.
9. Дополнительно исправь hero-счётчики: в HTML должны сразу отображаться реальные значения `22` и `40`, а не `0`, чтобы без JavaScript и в поисковой выдаче не было «0 года стажа» и «0 отзывов».
10. После правки кратко перечисли, какие CSS/JS/HTML-участки были изменены.
